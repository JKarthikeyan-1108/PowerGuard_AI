// Root page redirect
// The middleware actually handles the redirection for the root path ('/'),
// but Next.js requires a page component at the root.

export default function RootPage() {
  return null;
}
